# Handoff - 拼多多真题构建任务交接

> 生成时间：2026-08-17。本文件用于回家后继续任务，可交给 Codex 直接按步骤执行。

## 一、任务背景

用户已在 `pdd/` 下建好专项目录骨架（提交 `8b10697`），当前任务：

1. 搜索拼多多笔试**真题原文**；找不到完整原文时，根据 `pdd/README.md` 题单概述 + 网络公开片段，为 8 个题目目录构建**重构版练习题目** `problem.md`（只写题目、样例、数据范围，不写题解）；
2. 把每题样例 1 填入 `input.txt` / `expected.txt`；
3. 可选：更新根 `README.md` 的 pdd 说明；
4. 提交并推送（commit message 用中文）。

## 二、当前状态（2026-08-17）

### 已完成

- 搜索确认：牛客 / CSDN 等全文被反爬拦截，**未找到任何官方原文**，只有搜索摘要片段；基于片段已确认 8 题的核心模型。
- **7 个 `problem.md` 已写入且完整**（UTF-8，格式参照 `mhy/2026-03-14/Q1/problem.md`）：

| 文件 | 状态 |
|---|---|
| `pdd/2026-03-15/Q1/problem.md` | 已写入（86 行） |
| `pdd/2026-03-29/Q1/problem.md` | 已写入（77 行） |
| `pdd/2026-03-29/Q2/problem.md` | 已写入（72 行） |
| `pdd/2026-03-29/Q3/problem.md` | 已写入（73 行） |
| `pdd/2026-04-12/Q1/problem.md` | 已写入（72 行） |
| `pdd/2026-04-12/Q2/problem.md` | 已写入（70 行） |
| `pdd/2026-06-16/Q2/problem.md` | 已写入（90 行） |

### 未完成（按顺序执行）

1. `pdd/2025-08-03/Q3/problem.md` **缺失**（仍处于 git 删除状态），需要重建；
2. 8 组 `input.txt` / `expected.txt` 全部为空，需要填入样例 1；
3. 根 `README.md` 的 pdd 部分说明（可选，建议做）；
4. `git add -A && git commit && git push`（中文 commit message）。

## 三、剩余步骤详细内容

### 步骤 1：重建 `pdd/2025-08-03/Q3/problem.md`

完整内容如下（直接写入即可）：

```markdown
# Q3 - 单峰序列最小代价（双向 DP）

> ⚠️ 非官方原题：拼多多原题未公开。以下为根据题单概述与网络公开信息重构的练习版题目，样例与数据范围为构造值；若之后找到官方原题可替换本文件。
> 来源：题单见 [../../README.md](../../README.md)

## 题目描述

小 P 有一个长度为 n 的整数序列 a[1..n]。每次操作可以把**任意一个元素的值加 1**，花费 1 的代价（元素只能增加，不能减少）。

目标是让整个序列变成“单峰”：存在一个峰顶下标 k（1 ≤ k ≤ n），使得：

- a[1] < a[2] < ... < a[k]（前半部分严格递增）
- a[k] > a[k+1] > ... > a[n]（后半部分严格递减）

当 k = 1 时表示整体严格递减；当 k = n 时表示整体严格递增。

请你计算：使序列变成单峰所需的最小总操作次数。

## 输入格式

第一行一个整数 n。

第二行 n 个整数 a[1], a[2], ..., a[n]。

## 输出格式

输出一个整数，表示最小总操作次数。

## 样例

### 样例 1

输入：

```text
3
1 2 1
```

输出：

```text
0
```

解释：序列已经是单峰（1 < 2 > 1），无需操作。

### 样例 2

输入：

```text
3
3 2 3
```

输出：

```text
2
```

解释：把中间元素从 2 加到 4，得到 3 < 4 > 3，花费 2；任何小于 2 的代价都无法同时满足左增右减。

## 数据范围

- 1 ≤ n ≤ 10^5
- 0 ≤ a[i] ≤ 10^9

## 注意事项

- 只能增加，不能减少。
- 递增和递减都是**严格**的，相邻元素至少相差 1。
- 峰顶可以出现在端点，即允许整体单调。
- 可以分别考虑“使前缀严格递增的最小代价”和“使后缀严格递减的最小代价”，再枚举峰顶合并。
- 答案可能超过 32 位整数范围，注意使用 64 位类型。
```

### 步骤 2：填写 8 组 `input.txt` / `expected.txt`

均为 UTF-8 纯文本，文件末尾带换行。内容如下（即每题样例 1）：

| 目录 | input.txt | expected.txt |
|---|---|---|
| `pdd/2026-03-15/Q1` | `4` / `1001 500 10` / `1002 300 8` / `1001 400 12` / `1003 500 5` | `3` / `1001 2` / `1002 3` / `1003 1` |
| `pdd/2026-03-29/Q1` | `5 10 2` / `2 3` / `1 4` | `YES 9` |
| `pdd/2026-03-29/Q2` | `3` / `0 1 2` | `3` |
| `pdd/2026-03-29/Q3` | `6 2` / `1 1 2 3 5 6` | `6` |
| `pdd/2026-04-12/Q1` | `3 6` / `1 3` / `2 5` / `3 6` | `3 3` |
| `pdd/2026-04-12/Q2` | `5 3` / `1 3 6 9 11` | `5` |
| `pdd/2026-06-16/Q2` | `3` / `1 2 1` | `1` |
| `pdd/2025-08-03/Q3` | `3` / `1 2 1` | `0` |

### 步骤 3：更新根 `README.md`（可选但建议）

1. 目录结构 pdd 部分，改为与 mhy 一致、标注已内置题目：

```text
│   ├── 2026-03-15/Q1/
│   │   ├── problem.md  # 题目描述（已内置练习版）
│   │   ├── main.cpp
│   │   ├── input.txt
│   │   ├── expected.txt
│   │   └── notes.md
│   ├── 2026-03-29/Q1,Q2,Q3/   # 同上结构
│   ├── 2026-04-12/Q1,Q2/
│   ├── 2026-06-16/Q2/
│   └── 2025-08-03/Q3/
```

2. pdd 说明行改为：

```text
> pdd 下每个 Q 目录结构与 mhy 相同：`problem.md`（已内置重构版练习题目）+ `main.cpp` + `input.txt`（样例 1）+ `expected.txt` + `notes.md`。
```

3. “如何开始一道新题”第 2 条改为：

```text
若题目描述缺失，把完整题目粘贴到该题目录下的 `problem.md`（mhy Q1~Q3 与 pdd 8 道已内置重构版练习题目）。
```

### 步骤 4：提交并推送

```powershell
git add -A
git commit -m "pdd: 内置 8 道重构版练习题目并填入样例"
git push
```

注意：本环境 git 写操作（add / commit / push）需要审批；commit message 用中文。

## 四、8 道题设计摘要（模型均已确认）

| 目录 | 题目 | 核心模型 | 样例 1 验证 |
|---|---|---|---|
| `2026-03-15/Q1` | 直播排行榜 | 排序 + 哈希（每主播保留人气最高/时间最早一条，多键排序，id→rank） | 4 条记录 → 3 主播，1003 第 1 |
| `2026-03-29/Q1` | 货运巡线 | 模拟（先卸后装，检查 0 ≤ 载重 ≤ maxWeight） | 5→3→6→5→9，YES 9 |
| `2026-03-29/Q2` | 排课计划 | DAG / 森林最长路径（每课至多一个先修，答案 = 最长链长度） | 链 1→2→3 → 3 |
| `2026-03-29/Q3` | 两个背包装奖品 | 排序 + 双指针 + 后缀最大值（两个不相交连续段，段内极差 ≤ k，最大总长；不是 0/1 背包） | 6 2 / 1 1 2 3 5 6 → 6 |
| `2026-04-12/Q1` | 赛道计时 | 差分数组 + 前缀和（闭区间覆盖次数最大，平局取最小编号） | 3 6 → 3 3 |
| `2026-04-12/Q2` | 站位安排 | 二分答案 + 贪心（选 K 点最大化最小相邻距离） | 5 3 → 5 |
| `2026-06-16/Q2` | 删除数组 | DP + 哈希优化（删 [i,j] 要求 a[i]==a[j]，最小操作数，O(n²)→O(n)） | 1 2 1 → 1 |
| `2025-08-03/Q3` | 单峰序列最小代价 | 双向 DP（只能 +1，前缀严格增 + 后缀严格减，枚举峰顶） | 1 2 1 → 0 |

## 五、关键规则（不要违反）

- **不写任何题解 / 算法实现**：`problem.md` 只含题目、输入输出格式、样例（≥2 个并带解释）、数据范围、注意事项（可含建模提示，参照 mhy 格式）。
- `main.cpp` 保持 ACM 空模板（`templates/acm.cpp`），**不要**填解法。
- `notes.md` 复盘模板已填好，不要改动内容。
- `pdd/README.md` 是用户自己的题单，**不要修改**。
- 8 个 `problem.md` 头部必须带“⚠️ 非官方原题”说明 + `来源：题单见 [../../README.md](../../README.md)`。
- 构造题数据范围一律让 `long long` 必要（涉及 10^9 级数值）。

## 六、环境与验证

- Windows + PowerShell 5.1 + MinGW g++（C++17），仓库根目录：`C:\Users\BHJ4SZH\Desktop\Study\acm-coding\ACM-coding`
- 每题运行验证：

```powershell
.\scripts\run.ps1 .\pdd\2026-03-15\Q1
```

脚本会自动编译（约 20~30 秒）、以 `input.txt` 为 stdin 运行、显示输出并和 `expected.txt` 比对。

- 手工编译运行：

```powershell
g++ -std=c++17 -O2 .\pdd\2026-03-15\Q1\main.cpp -o .\pdd\2026-03-15\Q1\main.exe
cmd /c ".\pdd\2026-03-15\Q1\main.exe < .\pdd\2026-03-15\Q1\input.txt"
```

- Git 当前分支 `main`，最近提交 `8b10697`；工作区脏状态 = 7 个 problem.md 修改 + 1 个 problem.md 删除（待重建），均为本任务预期改动。
